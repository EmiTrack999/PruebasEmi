<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inicio de Sesión - Biblioteca</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #004080, #008000);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .login-container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            text-align: center;
        }
        .login-container h1 {
            color: #004080;
            margin-bottom: 20px;
        }
        .login-container input[type="text"],
        .login-container input[type="password"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        .login-container button {
            background-color: #008000;
            color: #ffffff;
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }
        .login-container button:hover {
            background-color: #005700;
        }

        .volver-btn {
            margin-top: 10px;
            display: inline-block;
            background-color: #004080;
            color: white;
            padding: 8px 16px;
            border-radius: 4px;
            text-decoration: none;
            font-weight: bold;
        }
        .volver-btn:hover {
            background-color: #002d5a;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1>Registrarse</h1>
        <form action="/registrar" method="POST" onsubmit="return validarCorreo()">
            <input type="text" name="usuario" placeholder="Usuario" required>
            <input type="password" name="contrasena" placeholder="Contraseña" required>
            <button type="submit">Registrar</button>
            <% if (mensaje) { %>
                <p style="color: red;"><strong><%= mensaje %></strong></p>
            <% } %>
        </form>
        <script>
            function validarCorreo() {
                const usuario = document.querySelector('input[name="usuario"]').value;
                const regex = /^[a-zA-Z0-9._%+-]+@(gmail|yahoo|outlook)\.com$/i;
                if (!regex.test(usuario)) {
                    alert('Por favor, use un correo de Gmail, Yahoo o Outlook.');
                    return false;
                }
                return true;
            }
        </script>
        <a href="/login" class="volver-btn">Volver</a>
    </div>
</body>
</html>
