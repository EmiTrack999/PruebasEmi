<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Mensajes de Ayuda</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #4ca4fc, #e3f2fd);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        h1 {
            color: #0d6efd;
        }
        table {
            background-color: white;
        }
        th {
            background-color: #0d6efd;
            color: white;
        }
        td {
            vertical-align: middle;
        }
        .btn-secondary {
            background-color: #6c757d;
            border: none;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
        }
    </style>
</head>
<body s>
    <div class="container mt-5 shadow p-4 bg-white rounded">
        <h1 class="text-center mb-4">📩 Mensajes de Ayuda</h1>

        <% if (ayuda.length === 0) { %> 

            <div class="alert alert-warning text-center">No hay mensajes de ayuda.</div>
        <% } else { %> 
            <table class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Correo</th>
                        <th>Mensaje</th>
                    </tr>
                </thead>
                <tbody>
                    <% ayuda.forEach(mensaje => { %>
                        <tr>
                            <td><%= mensaje.nombre %></td>
                            <td><%= mensaje.correo %></td>
                            <td><%= mensaje.mensaje %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>

        <div class="text-center mt-4">
            <a href="/administracion" class="btn btn-secondary">← Volver al Panel</a>
        </div>
    </div>
</body>
</html>
